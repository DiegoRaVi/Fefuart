# Notifications module

This module owns operational notifications for users.

Current vertical slice implemented:

- GET /api/v1/notifications/my
- PATCH /api/v1/notifications/{id}/read
- backoffice status changes create in-app notifications for affected users
