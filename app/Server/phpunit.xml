<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Fijado aqui y no heredado del .env: los tests que comprueban un
             mensaje no pueden depender del idioma que tenga configurado quien
             los ejecuta. -->
        <env name="APP_LOCALE" value="es"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- SQLite en memoria. Sin esto los tests corren contra la BD de
             desarrollo `fefuart` y el primer RefreshDatabase la vacia. -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- `array` no persiste la sesion entre peticiones, asi que no
             ejercita la autenticacion por cookie de Sanctum (D2). Con
             `database` los tests recorren el mismo camino que la aplicacion;
             RefreshDatabase crea la tabla `sessions`. -->
        <env name="SESSION_DRIVER" value="database"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
